# Leave unset for local Vite dev so requests use vite.config.ts proxy.
# Set only when serving the dashboard from a different origin with CORS enabled.
# VITE_INFERD_API=https://your-inferd-host.example
